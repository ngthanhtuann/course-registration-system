import React,{useEffect,useState}from'react';import{Card,SearchBox,DataTable,SectionHeader,Modal,Button,Badge}from'../../components/ui';import type{Column}from'../../components/ui';import{api}from'../../api';import type{Student}from'../../types';export default function ViewCourses({user}:{user:Student}){const[rows,setRows]=useState<any[]>([]);const[search,setSearch]=useState('');const[view,setView]=useState<any>(null);useEffect(()=>{api.student.courses('',search).then(setRows).catch(()=>setRows([]))},[search]);const cols:Column<any>[]=[{key:'course_code',header:'Course Code',render:r=><span className="font-mono text-xs">{r.course_code}</span>},{key:'course_name',header:'Course Name',render:r=><span className="font-medium">{r.course_name}</span>},{key:'credit',header:'Credits'},{key:'recommended_semester',header:'Recommended Semester'},{key:'prerequisite_status',header:'Prerequisite',render:r=><Badge label={r.prerequisite_status==='N/A'?'Satisfied':r.prerequisite_status}/>},{key:'action',header:'Action',render:r=><Button size="sm" variant="ghost" onClick={()=>setView(r)}>View</Button>}];return <div><SectionHeader title="View Courses" subtitle={`Courses in the ${user.major} curriculum`}/><Card className="p-4 mb-4"><SearchBox value={search} onChange={setSearch} placeholder="Search by course code or name…"/></Card><Card><DataTable columns={cols} rows={rows} keyFn={r=>r.course_code} emptyText="No courses available."/></Card><Modal open={!!view} onClose={()=>setView(null)} title="Course Details" footer={<Button variant="secondary" onClick={()=>setView(null)}>Close</Button>}>{view&&<div className="grid grid-cols-2 gap-3">{[['Code',view.course_code],['Name',view.course_name],['Credits',view.credit],['Prerequisite',view.prerequisite_name||'None'],['Recommended Semester',view.recommended_semester],['Capacity',view.max_capacity]].map(([k,v])=><div key={String(k)} className="bg-slate-50 p-3 rounded"><p className="text-xs text-slate-500">{k}</p><p className="text-sm font-semibold">{v}</p></div>)}</div>}</Modal></div>}
