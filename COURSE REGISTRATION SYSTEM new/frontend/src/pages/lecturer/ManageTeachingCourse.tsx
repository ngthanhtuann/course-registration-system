import React,{useEffect,useState}from'react';import{Card,Select,DataTable,SectionHeader,Modal,Button}from'../../components/ui';import type{Column}from'../../components/ui';import{api}from'../../api';import type{Lecturer}from'../../types';export default function ManageTeachingCourse({user}:{user:Lecturer}){const[sems,setSems]=useState<any[]>([]);const[semester,setSemester]=useState('');const[courses,setCourses]=useState<any[]>([]);const[view,setView]=useState<any>(null);const[students,setStudents]=useState<any[]>([]);useEffect(()=>{api.lecturer.semesters().then(s=>{setSems(s);if(s[0])setSemester(s[0].semester_id)}).catch(()=>{})},[]);useEffect(()=>{if(semester)api.lecturer.teachingCourses(semester).then(setCourses).catch(()=>setCourses([]))},[semester]);const open=async(c:any)=>{setView(c);try{setStudents(await api.lecturer.students(c.course_code,semester))}catch{setStudents([])}};const cols:Column<any>[]=[{key:'course_code',header:'Course Code',render:r=><span className="font-mono text-xs">{r.course_code}</span>},{key:'course_name',header:'Course Name',render:r=><span className="font-medium">{r.course_name}</span>},{key:'credit',header:'Credits'},{key:'student_count',header:'Students'},{key:'action',header:'Action',render:r=><Button size="sm" variant="ghost" onClick={()=>open(r)}>View Students</Button>}];return <div><SectionHeader title="Manage Teaching Courses" subtitle="Courses assigned to you for the selected semester"/><Card className="p-4 mb-4"><Select label="Semester" options={sems.map(s=>({value:s.semester_id,label:s.semester_name}))} value={semester} onChange={e=>setSemester(e.target.value)}/></Card><Card><DataTable columns={cols} rows={courses} keyFn={r=>r.assignment_id} emptyText="No teaching courses found."/></Card><Modal open={!!view} onClose={()=>setView(null)} title={`Students — ${view?.course_code||''}`} size="lg" footer={<Button variant="secondary" onClick={()=>setView(null)}>Close</Button>}><DataTable columns={[{key:'student_id',header:'Student ID'},{key:'fullname',header:'Name'},{key:'email',header:'Email'},{key:'grade',header:'Grade',render:r=>r.grade??'Not entered'}]} rows={students} keyFn={r=>r.registration_id} emptyText="No registered students."/></Modal></div>}
